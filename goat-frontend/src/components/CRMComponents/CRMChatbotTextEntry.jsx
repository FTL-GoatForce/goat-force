import React from "react";

const CRMChatbotTextEntry = ({ sender, content }) => {
  return (
    <div>
      {/* have some conditional rendering where if it is an ai response we position to the left with a different color else to the right with different color  */}
    </div>
  );
};

export default CRMChatbotTextEntry;
